# SMTP Configuration (for future use)
SMTP_HOST=
SMTP_PORT=
SERPAPI_KEY=
# SMTP Configuration
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test@example.com
SMTP_PASS=
# IMAP Configuration
IMAP_HOST=localhost
IMAP_USER=test@example.com
IMAP_PASS=
